/*
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class Datetime {
	public static void main(String [] args) {
		DateTimeFormatter sdf = DateTimeFormatter.ofPattern("dd-MMMM");
		LocalDate date1 = LocalDate.of(0, 12, 12);
		System.out.println(sdf.format(date1));
		if(date1.isAfter());
	}
}


import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class datetime{
	public static void main(String [] args){
	
		int month = 10;
		System.out.println(Month.of(month).name().toLowerCase());
		
		DateTimeFormatter sdf = DateTimeFormatter.ofPattern("dd-MMMMM");
		LocalDate date1 = LocalDate.of(12, 12);
		System.out.println(sdf.date1);
	}
}
*/